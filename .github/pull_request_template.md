## What does this change?

<!-- Describe the behaviour change, not just the diff. -->

## Why?

<!-- Link the issue, or explain the problem being solved. -->

## Checklist

- [ ] Tests added or updated, and they inject `fetch` rather than calling the gateway
- [ ] `npm run type-check` passes
- [ ] `npm test` passes
- [ ] `npm run build` passes
- [ ] `CHANGELOG.md` updated under `## [Unreleased]`
- [ ] No credentials, live session IDs or cardholder data in the diff
